export default async function handler(req,res){const key=process.env.RAILRADAR_API_KEY;if(!key)return res.status(500).json({error:"RAILRADAR_API_KEY is missing"});const p=(new URL(req.url,"http://localhost").searchParams.get("pnr")||"").replace(/\D/g,"");if(!/^\d{10}$/.test(p))return res.status(400).json({error:"Valid 10 digit PNR required"});try{const r=await fetch(`https://api.railradar.in/v1/pnr/${p}`,{headers:{Authorization:`Bearer ${key}`,Accept:"application/json"}});const text=await r.text();let b;try{b=JSON.parse(text)}catch{b={error:text.slice(0,500)}}return res.status(r.status).json(b)}catch(e){return res.status(502).json({error:e.message})}}